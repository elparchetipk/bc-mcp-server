# .env.example
# Este archivo sirve como plantilla para las variables de entorno.
# Copie este archivo a .env y rellene los valores.
# El archivo .env NO debe ser subido a Git.

# Configuración de la base de datos PostgreSQL
# Utilizada por el servicio 'db' en docker-compose.yml
POSTGRES_USER=bootcamp_user
POSTGRES_PASSWORD=supersecretpassword
POSTGRES_DB=mcp_bootcamp_db
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"

# Configuración del entorno de Node.js
NODE_ENV=development
